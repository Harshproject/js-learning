//window->document->html->head, body....

// document.getElementsByClassName("mw-heading mw-heading2").innerHTML="<p>hehe</p>"

//document.querSelector(".class1")
//const my_ul= doument.querySelector('ul')
//const turn_green = my_ul.querySelecetor('li')
//turn_green.style.backgroundColor="green"
//turnGreen.style.padding="10px"
//turnGreen.innerText="five"

//If you want to see what functions does a particular selector contains then check its prototype

//There are two types of list in window HTML collection and nodeList which behaves quite similar to normal array
//But HTML collection doesnt contains foreach method so instead we will do this
//Array.from(htmlcollectionlist)
//Now this a normal array
